# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Immobilier France (DVF)"
slug: "immobilier-france"

runtime: typescript

build:
  install: "npm install"
  command: "npm run build"

start:
  type: stdio
  command: "python server.py"

bridge:
  mode: stdio
